# 允许 @nestjs/core 构建脚本（pnpm 9+ 默认阻止 native 构建）
auto-install-peers=true
strict-peer-dependencies=false
# 跳过构建脚本检查（避免 ERR_PNPM_IGNORED_BUILDS 阻断）
ignore-build-scripts=true
# pnpm 11+ 默认 run 脚本前会自动 install 检查；改为 warn 避免阻断 test/build
verify-deps-before-run=false
